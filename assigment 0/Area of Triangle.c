//Area of Triangle
//Area = (base × height) / 2

#include <stdio.h>

int main() {
    float base = 10;
    float height = 5;

    float area = (base * height) / 2;

    printf("Area = %.2f", area);

    return 0;
}